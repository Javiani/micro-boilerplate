{
  "name": "micro-boilerplate",
  "version": "0.0.0",
  "description": "A minimalist Boilerplate for super-fast and simple apps",
  "main": "index.pug",
  "scripts": {
    "start": "parcel index.pug --open --port 3000",
    "build": "parcel build index.pug"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "jails-js": "^3.3.5",
    "jails.packages": "jails-org/packages"
  },
  "devDependencies": {
    "parcel-bundler": "^1.11.0",
    "pug": "^2.0.3",
    "stylus": "^0.54.5"
  }
}
